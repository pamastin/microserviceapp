apiVersion: v1
kind: Service
metadata:
  name: {{ include "coit-frontend.fullname" . }}
  labels:
    {{- include "coit-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "coit-frontend.selectorLabels" . | nindent 4 }}
